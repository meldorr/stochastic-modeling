# Fully-connected U-Net on flattened (C*T), standard scaler, x/y features.
features: xy
arch: fcn_unet
scaler: standard
dropout: 0.0
training:
  epochs: 19000    # legacy fcn runs converged ~19k epochs
