# Runtime deps for the full pipeline + ablation study.
# NOTE: the `traffic` library is NOT needed when data/processed.npz is present
# (only Stage-1 prepare needs it, and processed.npz ships pre-built).
# Versions below are the ones this repo was developed/tested against; recent
# versions should also work. Install torch with CUDA per pytorch.org for GPU.
torch>=2.5
numpy>=2.0
scipy>=1.15
scikit-learn>=1.7
pandas>=2.3
pyarrow>=17
matplotlib>=3.10
pyproj>=3.6
PyYAML>=6.0
tensorboard>=2.18
tqdm>=4.66
pytest>=8.0
